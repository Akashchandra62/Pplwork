<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("./partials/head.ejs")%>
    <title>Employee</title>
</head>
<link>
<%- include("./partials/nav.ejs")%>

<form>
    <h2>Create New Employee</h2>
    <div>
        <input type="text" id="name" name="name" placeholder="Enter the Employee name" value="<%=user.name%>">
    </div>
    <div>
        <input type="email" id="email" name="email" placeholder="Enter the Employee email" value="<%=user.email%>">
    </div>

    <button class="btn" value="<%=user._id%>">Save Changes</button>
</form>
<%- include("./partials/script.ejs")%>
<!-- <script>
    async function handleSubmit(e) {
        e.preventDefault();
        const data = Object.fromEntries(new FormData(e.target).entries());
        const id = document.getElementsByClassName("btn")[0].value ;
        // Fetching data from Backend
        let res = await fetch(`/edit/${id}`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
        })
        res = await res.json();
        console.log(res);
        if (res.message == "success") {
            // location("/");

            window.location.replace("/");
        }
    }
    const form = document.querySelector("form");
    form.addEventListener("submit", handleSubmit)
</script> -->

</body>

</html>